{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty' 2>/dev/null); case \"$f\" in *SKILL.md) p=$(command -v python || command -v python3); [ -n \"$p\" ] && \"$p\" .claude/hooks/check_frontmatter.py \"$f\" ;; esac",
            "timeout": 10,
            "if": "Edit(**/SKILL.md)"
          }
        ]
      }
    ]
  }
}
